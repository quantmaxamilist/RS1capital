<section
  id="hero"
  class="relative min-h-svh flex items-center justify-center overflow-hidden bg-gradient-to-b from-panel to-black pt-[calc(var(--announcement-height,40px)+5rem)] md:pt-[calc(var(--announcement-height,40px)+5.5rem)]"
  aria-label="Hero"
>
  <div
    id="hero-glow"
    class="hero-horizon absolute inset-0 pointer-events-none z-0 overflow-hidden opacity-0"
    aria-hidden="true"
  >
    <div class="hero-horizon-stage">
      <div class="hero-horizon-underglow"></div>
      <div class="hero-horizon-bloom-anchor hero-horizon-bloom-secondary-anchor">
        <div class="hero-horizon-bloom hero-horizon-bloom-secondary"></div>
      </div>
      <div class="hero-horizon-bloom-anchor hero-horizon-bloom-primary-anchor">
        <div class="hero-horizon-bloom hero-horizon-bloom-primary"></div>
      </div>
      <div class="hero-horizon-rim-anchor">
        <div class="hero-horizon-rim-hot"></div>
      </div>
      <div class="hero-planet-disc">
        <div class="hero-planet-rim-shimmer" aria-hidden="true"></div>
      </div>
    </div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-6 lg:px-10 text-center py-20">
    <p
      id="hero-eyebrow"
      class="hero-eyebrow eyebrow mb-8 opacity-0 tracking-[0.35em]"
    >
      SPEED • CERTAINTY • PRAGMATISM
    </p>

    <h1 class="hero-headline heading-display text-[clamp(2.75rem,7vw,4.5rem)] text-mist mb-8">
      <span class="hero-line block opacity-0 translate-y-8">Capital and expertise</span>
      <span class="hero-line block opacity-0 translate-y-8">for critical moments.</span>
    </h1>

    <p
      id="hero-sub"
      class="hero-sub font-body text-base md:text-lg text-mist/70 max-w-2xl mx-auto leading-relaxed opacity-0"
    >
      RS1 Capital Limited is a specialist investment, lending and advisory firm supporting
      businesses across the UK and Europe — providing capital, operational turnaround and strategic
      advice where speed, certainty and pragmatism matter, regardless of financial status.
    </p>
  </div>
</section>

<style>
  .hero-headline {
    letter-spacing: -0.015em;
  }

  .hero-horizon-stage {
    position: absolute;
    left: 50%;
    bottom: -234vw;
    width: 260vw;
    aspect-ratio: 1;
    transform: translateX(-50%);
  }

  /* Light source beneath the planet */
  .hero-horizon-underglow {
    position: absolute;
    left: 50%;
    top: 0;
    width: 85%;
    height: 32%;
    transform: translate(-50%, -38%);
    background: radial-gradient(
      ellipse 60% 90% at 50% 100%,
      rgba(255, 244, 214, 0.32) 0%,
      rgba(201, 162, 39, 0.26) 35%,
      rgba(111, 82, 16, 0.14) 58%,
      transparent 78%
    );
    filter: blur(32px);
  }

  /* Atmosphere bloom — soft sky glow above the rim */
  .hero-horizon-bloom-anchor {
    position: absolute;
    left: 50%;
    top: 0;
    pointer-events: none;
  }

  .hero-horizon-bloom-secondary-anchor {
    transform: translate(-50%, -72%);
  }

  .hero-horizon-bloom-primary-anchor {
    transform: translate(-50%, -118%);
  }

  .hero-horizon-bloom {
    will-change: transform, opacity;
    transform-origin: 50% 100%;
  }

  .hero-horizon-bloom-primary {
    width: min(200vw, 1200px);
    height: min(130vh, 900px);
    background: radial-gradient(
      ellipse 58% 68% at 50% 100%,
      rgba(255, 244, 214, 0.62) 0%,
      rgba(255, 244, 214, 0.38) 8%,
      rgba(201, 162, 39, 0.42) 22%,
      rgba(201, 162, 39, 0.22) 38%,
      rgba(111, 82, 16, 0.14) 52%,
      rgba(111, 82, 16, 0.05) 64%,
      transparent 82%
    );
    filter: blur(44px);
  }

  .hero-horizon-bloom-secondary {
    width: min(240vw, 1400px);
    height: min(95vh, 680px);
    background: radial-gradient(
      ellipse 72% 62% at 50% 100%,
      rgba(201, 162, 39, 0.28) 0%,
      rgba(111, 82, 16, 0.16) 38%,
      rgba(111, 82, 16, 0.06) 55%,
      transparent 78%
    );
    filter: blur(64px);
  }

  /* Drifting hot highlight along the crest */
  .hero-horizon-rim-anchor {
    position: absolute;
    left: 50%;
    top: 0;
    transform: translate(-50%, -52%);
    pointer-events: none;
  }

  .hero-horizon-rim-hot {
    width: min(38vw, 320px);
    height: min(3.5vw, 28px);
    background: radial-gradient(
      ellipse 100% 100% at 50% 100%,
      rgba(255, 244, 214, 0.98) 0%,
      rgba(255, 244, 214, 0.5) 30%,
      rgba(201, 162, 39, 0.2) 55%,
      transparent 78%
    );
    filter: blur(5px);
    will-change: transform, opacity;
    transform-origin: 50% 100%;
  }

  /* Planet body — black disc occluding glow below the rim */
  .hero-planet-disc {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background: #080808;
    box-shadow:
      0 -1px 0 rgba(255, 244, 214, 0.96),
      0 -2px 8px rgba(255, 244, 214, 0.72),
      0 -8px 26px rgba(255, 244, 214, 0.38),
      0 -18px 52px rgba(201, 162, 39, 0.48),
      0 -40px 100px rgba(111, 82, 16, 0.22);
  }

  .hero-planet-rim-shimmer {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    height: 2px;
    border-radius: 50% 50% 0 0;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 244, 214, 0.55) 18%,
      rgba(255, 244, 214, 0.98) 50%,
      rgba(255, 244, 214, 0.55) 82%,
      transparent 100%
    );
    box-shadow:
      0 0 12px rgba(255, 244, 214, 0.55),
      0 -4px 28px rgba(201, 162, 39, 0.35);
    will-change: opacity;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-horizon-bloom,
    .hero-horizon-rim-hot,
    .hero-planet-rim-shimmer {
      will-change: auto;
    }
  }
</style>
