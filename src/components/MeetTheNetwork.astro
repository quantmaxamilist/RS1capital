---
import { audienceCards } from '../data/site';
---

<section id="who-we-work-with" class="bg-white py-24 lg:py-32" aria-labelledby="audience-heading">
  <div class="max-w-7xl mx-auto px-6 lg:px-10">
    <div class="flex items-start justify-between gap-6 mb-12">
      <h2 id="audience-heading" class="heading-display text-[clamp(2rem,4vw,3rem)] text-black">
        Who We Work With
      </h2>
    </div>

    <div id="network-rail" class="network-rail mb-10" role="list">
      {
        audienceCards.map((card) => (
          <article
            class="w-64 md:w-72 rounded-xl overflow-hidden relative bg-black shrink-0 p-8 flex flex-col justify-end min-h-[16rem]"
            role="listitem"
          >
            <div class="absolute inset-0 bg-gradient-to-br from-charcoal via-black to-gold/10" aria-hidden="true"></div>
            <div class="absolute top-6 right-6 w-12 h-12 hatch-corner rounded-sm opacity-60" aria-hidden="true"></div>
            <div class="relative">
              <h3 class="font-display text-xl font-semibold text-gold mb-3">{card.title}</h3>
              <p class="font-body text-sm text-mist/75 leading-relaxed">{card.description}</p>
            </div>
          </article>
        ))
      }
    </div>

    <div class="text-center">
      <a
        href="#contact"
        class="inline-block font-body text-xs font-semibold tracking-[0.16em] uppercase bg-gold text-black px-8 py-3.5 rounded-sm hover:bg-gold-2 transition-colors"
      >
        Get in touch
      </a>
    </div>
  </div>
</section>

<script>
  const rail = document.getElementById('network-rail');
  if (rail) {
    let isDown = false;
    let startX = 0;
    let scrollLeft = 0;

    rail.addEventListener('mousedown', (e: MouseEvent) => {
      isDown = true;
      startX = e.pageX - rail.offsetLeft;
      scrollLeft = rail.scrollLeft;
    });

    rail.addEventListener('mouseleave', () => {
      isDown = false;
    });

    rail.addEventListener('mouseup', () => {
      isDown = false;
    });

    rail.addEventListener('mousemove', (e: MouseEvent) => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - rail.offsetLeft;
      const walk = (x - startX) * 1.5;
      rail.scrollLeft = scrollLeft - walk;
    });
  }
</script>
